node_modules/
# dist/ is intentionally tracked so jsDelivr can serve via /gh/ path on tag.
# CI rebuilds + commits dist/ on every release. Only the bundled outputs are
# tracked — TypeScript declaration trees + per-module artifacts stay ignored.
dist/index.d.ts
dist/core/
dist/dom/
dist/gates/
dist/renderers/
coverage/
test-results/
playwright-report/
*.log
.DS_Store
.env
.env.*
!.env.example
.agents/
.claude/
skills-lock.json
